# Module Summary — {module-name}

**Module:** {module-name}
**Type:** Standard API Module | Web UI | Mobile App | UI Component Library
**Feature:** {feature-name}
**Generated by:** Milo `/module-summary`
**Date:** {date}
**Status:** PENDING APPROVAL

---

## What Was Built

{One to two paragraphs. Describe what was implemented in domain language. What does this module own? What can the rest of the system now do that it couldn't before?}

---

## API Exports

> Standard API modules only. Web UI / Mobile App: list pages/screens built. UI Component Library: list components exported.

### Exported Functions (Final Signatures)

```typescript
export async function {functionName}(
  {param}: {Type}
): Promise<{ReturnType}>
```

{Description — one sentence}

---

{Repeat for each exported function}

### Generated REST Endpoints

| Endpoint | Method | Request Body | Response |
|----------|--------|-------------|---------|
| `/api/{module-name}/{functionName}` | POST | `{ {param}: {type} }` | `{ {field}: {type} }` |

---

## Dependencies Used

| Module | Functions Called | Purpose |
|--------|----------------|---------|
| {module-name} | `{functionName}`, `{functionName}` | {purpose} |

---

## OSS Packages Used

| Package | Version | Category | Conforms to Standard |
|---------|---------|----------|---------------------|
| {package} | {version} | {category} | ✓ / ✗ (deviation noted below) |

**Package deviations (if any):**

{List any packages used that are not in `global-picture/standards/oss-packages.md`, with justification.}

---

## Deployment Abstraction

> Standard API modules only.

| Layer | File | Status |
|-------|------|--------|
| Function Exports | `modules/{module-name}/index.ts` | ✓ Complete |
| REST API Server | `modules/{module-name}/server.ts` | ✓ Complete |
| Smart Call Wrapper | `modules/{module-name}/proxy.ts` | ✓ Complete |

**DEPLOYMENT_MODE routing confirmed:** Yes / No

**Notes:** {any implementation-specific details about the deployment abstraction}

---

## Test Coverage

| Test Type | Files | What's Covered |
|-----------|-------|---------------|
| Unit | `{file}` | {functions covered} |
| Integration | `{file}` | {endpoints / scenarios covered} |
| E2E | `{file}` | {user flows covered} |

**Coverage gaps:** {anything explicitly not tested and why}

---

## Deviations from Product Brief

{Any differences between the Product Brief specification and the actual implementation. If none, state "None."}

| Brief Specification | Actual Implementation | Justification |
|--------------------|-----------------------|---------------|
| {what was specified} | {what was built} | {why it changed} |

---

## Validation Results

| Check | Result | Notes |
|-------|--------|-------|
| API contract — all functions implemented | ✓ Pass / ✗ {issue} | |
| API contract — signatures match brief | ✓ Pass / ✗ {issue} | |
| REST endpoints — all generated | ✓ Pass / ✗ {issue} | |
| Smart call wrapper — implemented | ✓ Pass / N/A | |
| OSS packages — all from standards | ✓ Pass / ✗ {issue} | |
| Dependencies — imported via proxy | ✓ Pass / ✗ {issue} | |
| Tests — unit tests exist | ✓ Pass / ✗ {issue} | |
| No scope creep — no extra exports | ✓ Pass / ✗ {issue} | |

**Blocking issues:** {None / List any issues that prevented full approval}

---

## Notes

{Any additional context about the implementation — trade-offs made, things to revisit, technical debt acknowledged, etc.}
